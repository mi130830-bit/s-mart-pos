import 'dart:io'; import 'package:mysql_client_plus/mysql_client_plus.dart'; void main() async { print('Connecting...'); final conn = await MySQLConnection.createConnection(host: 'localhost', port: 3306, userName: 'root', password: '', databaseName: 's_mart_db'); await conn.connect(); var result = await conn.execute('SELECT id, user_id, display_name FROM employee_profile'); for(var row in result.rows) { print(row.assoc()); } await conn.close(); }
