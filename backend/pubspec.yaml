name: backend
description: A starting point for a Dart server.
version: 1.0.0

environment:
  sdk: ^3.10.4

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  mysql_client_plus: ^0.0.31
  dotenv: ^4.2.0
  decimal: ^3.0.1
  args: ^2.4.2
  dbcrypt: ^2.0.0
  dart_jsonwebtoken: ^2.12.0
  http: ^1.6.0
  shelf_static: ^1.1.3
  firedart: ^0.9.8  # Firestore SDK for Dart Server

dev_dependencies:
  lints: ^2.1.1
  test: ^1.24.9
