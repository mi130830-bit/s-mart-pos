import 'dart:io'; import 'package:mysql_client_plus/mysql_client_plus.dart'; void main() async { print('Connecting...'); final conn = await MySQLConnection.createConnection(host: 'localhost', port: 3306, userName: 'root', password: '!QAZ2wsx', databaseName: 's_mart_db'); await conn.connect(); var result = await conn.execute('SELECT a.id, a.employee_id, e.user_id, a.clock_in FROM attendance_log a JOIN employee_profile e ON a.employee_id = e.id WHERE a.date = CURDATE()'); for(var row in result.rows) { print(row.assoc()); } await conn.close(); }
