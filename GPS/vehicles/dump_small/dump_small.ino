// GPS tracker configuration — dump small / รถดั้มเล็ก
// Upload this sketch to the tracker installed in the small dump truck.
// The shared firmware remains in ../../GPS.ino so bug fixes apply to every vehicle.

#define GPS_VEHICLE_NAME "ดั้มเล็ก"
#include "../../GPS.ino"
