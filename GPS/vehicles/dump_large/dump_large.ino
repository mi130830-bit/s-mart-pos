// GPS tracker configuration — dump large / รถดั้มใหญ่
// Upload this sketch to the tracker installed in the large dump truck.
// The shared firmware remains in ../../GPS.ino so bug fixes apply to every vehicle.

#define GPS_VEHICLE_NAME "ดั้มใหญ่"
#include "../../GPS.ino"
